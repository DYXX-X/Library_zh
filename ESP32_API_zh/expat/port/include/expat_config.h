/* expat_config.h由configure从expat_conconfig.h.in生成。*/
/* expat_config.h.in.由autoheader从config.ac生成。*/

/* 1234 = LIL_ENDIAN, 4321 = BIGENDIAN */
#define BYTEORDER 1234
/* 如果具有“bcopy”函数，则定义为1。*/
#define HAVE_BCOPY 1

/* 如果有<dlfcn.h>头文件，则定义为1。*/
#define HAVE_DLFCN_H 1

/* 如果有<fcntl.h>头文件，则定义为1。*/
#define HAVE_FCNTL_H 1

/* 如果具有“getpagesize”函数，则定义为1。*/
#define HAVE_GETPAGESIZE 1
/* 如果有＜inttypes.h＞头文件，则定义为1。*/
#define HAVE_INTTYPES_H 1

/* 如果具有“memmove”函数，则定义为1。*/
#define HAVE_MEMMOVE 1

/* 如果有＜memory.h＞头文件，则定义为1。*/
#define HAVE_MEMORY_H 1

/* 如果有正在工作的“mmap”系统调用，则定义为1。*/
#define HAVE_MMAP 1

/* 如果有<stdint.h>头文件，则定义为1。*/
#define HAVE_STDINT_H 1

/* 如果有<stdlib.h>头文件，则定义为1。*/
#define HAVE_STDLIB_H 1

/* 如果有＜strings.h＞头文件，则定义为1。*/
#define HAVE_STRINGS_H 1

/* 如果有<string.h>头文件，则定义为1。*/
#define HAVE_STRING_H 1

/* 如果有<sys/param.h>头文件，则定义为1。*/
#define HAVE_SYS_PARAM_H 1

/* 如果有<sys/stat.h>头文件，则定义为1。*/
#define HAVE_SYS_STAT_H 1

/* 如果具有<sys/types.h>头文件，则定义为1。*/
#define HAVE_SYS_TYPES_H 1

/* 如果有＜unistd.h＞头文件，则定义为1。*/
#define HAVE_UNISTD_H 1

/* 定义到libtool存储卸载库的子目录。*/
#define LT_OBJDIR ".libs/"

/* 包装名称*/
#define PACKAGE "expat"

/* 定义发送此包的错误报告的地址。*/
#define PACKAGE_BUGREPORT "expat-bugs@libexpat.org"

/* 定义为此包的全名。*/
#define PACKAGE_NAME "expat"

/* 定义此包的全名和版本。*/
#define PACKAGE_STRING "expat 2.4.3"

/* 将此包的短名称定义为一个符号。*/
#define PACKAGE_TARNAME "expat"

/* 定义到此包的主页。*/
#define PACKAGE_URL ""

/* 定义为此包的版本。*/
#define PACKAGE_VERSION "2.4.3"

/* 如果有ANSI C头文件，则定义为1。*/
#define STDC_HEADERS 1

/* 软件包版本号*/
#define VERSION "2.4.3"

/* 字节序是否为双字节序*/
/* #undef WORDS_BIGENDIAN*/

/* 定义以指定在当前分析点周围保留多少上下文。*/
#define XML_CONTEXT_BYTES 1024

/* 定义以使参数实体分析功能可用。*/
#define XML_DTD 1

/* 定义以使XML命名空间功能可用。*/
#define XML_NS 1

/* 如果“const”不符合ANSI C，则定义为空。*/
/* #不变常数*/

/* 如果＜sys/types.h＞未定义，则定义为“long int”。*/
/* #未定义关闭*/

/* 如果＜sys/types.h＞未定义，则定义为“unsigned int”。*/
/* #未定义大小_*/

